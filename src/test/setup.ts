import "@testing-library/jest-dom";
import "jest-axe/extend-expect";

// Import OAuth mocks globally to prevent database connection errors
import "@/test/mocks/oauth-mailchimp";

// Add any global test setup here
